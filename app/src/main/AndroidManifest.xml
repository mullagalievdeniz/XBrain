<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.xbrain">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.XBrain">
        <activity
            android:name=".home"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.XBrain.NoActionBar" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.XBrain.NoActionBar" />
        <activity
            android:name=".Atoms"
            android:label="@string/title_activity_atoms"
            android:theme="@style/Theme.XBrain.NoActionBar" />
        <activity android:name=".time" />
        <activity android:name=".profile_activity" />
        <activity android:name=".Assistant_activivty" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>